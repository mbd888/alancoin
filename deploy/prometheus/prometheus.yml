global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - rules.yml

scrape_configs:
  - job_name: "alancoin"
    static_configs:
      - targets: ["alancoin:8080"]
    metrics_path: /metrics
